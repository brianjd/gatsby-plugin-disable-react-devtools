const { onRenderBody } = require(`../gatsby-ssr`)

define('gatsby-plugin-disable-react-devtools', () => {
  it('adds script if in production', () => {

  });
  it('does not add script if not in production', () => {

  });
});